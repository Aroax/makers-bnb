feature 'booking spaces' do

  scenario 'a user can request to book a space' do
      # add_space
      #
      # visit('/spaces')
      # first('.space').click_link('more details')
      # click_button('request to book')
      #
      # expect(current_path).to eq '/users/dashboard'
      #
      # expect(page).to have_content "Your Submitted Requests"
      # expect(page).to have_content "Pending:"
      # expect(page).to have_content "Approved:"
      # expect(page).to have_content "Declined:"
      # expect(page).to have_content "Dream House"
  end

end
